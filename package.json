{
  "name": "arham-fintech-portal",
  "version": "1.0.0",
  "description": "Stock Broking Internal Operations Portal — Mock BSE API + Internal Dashboard",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start:bse": "cd mock-bse-api && npm start",
    "start:server": "cd portal/server && npm start",
    "start:client": "cd portal/client && npm run dev",
    "dev:bse": "cd mock-bse-api && npm run dev",
    "dev:server": "cd portal/server && npm run dev",
    "dev:client": "cd portal/client && npm run dev",
    "install:all": "cd mock-bse-api && npm install && cd ../portal/server && npm install && cd ../client && npm install",
    "build:client": "npm --prefix portal/client run build",
    "test:backend": "npm --prefix mock-bse-api test && npm --prefix portal/server test",
    "test:client": "npm --prefix portal/client test",
    "test": "npm run test:backend && npm run test:client",
    "verify": "npm test && npm run build:client"
  }
}
